apiVersion: apps/v1
kind: Deployment
metadata:
     name: 1test11111
     labels:
        app: appsa-vi01
spec:
  replicas: 3
  selector:
      matchLabels:
          app: appsa-vi01
  template:
      metadata:
          labels:
               app: appsa-vi01
      spec:
               containers:
                - name: 11test1111
                  image: saikumarpanda28/testday1:latest
                  ports:
                    - containerPort: 8080
                    
                    
                    
apiVersion: v1
kind: Service
metadata:
  name: maven-apptest
  namespace: default
spec:
  selector:
    app: appsa-vi01
  type: LoadBalancer
  ports:
  - name: http
    targetPort: 8080
    port: 80
